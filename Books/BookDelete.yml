info:
  name: BookDelete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{URL}}/books/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
