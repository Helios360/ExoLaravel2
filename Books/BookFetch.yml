info:
  name: BookFetch
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/books
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
